<resources>

    <!-- Base application theme -->
    <style name="Theme.MyRecognitionApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Personnalise les couleurs principales de ton thème ici -->
        <item name="colorPrimary">@color/cyan</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <item name="android:statusBarColor">@color/cyan</item>
        <item name="colorAccent">@color/white</item>
    </style>
    <style name="CyanButton">
        <item name="android:backgroundTint">@color/cyan</item> <!-- Fond cyan -->
        <item name="android:textColor">@color/white</item> <!-- Texte blanc -->
        <item name="android:textStyle">bold</item> <!-- Texte en gras -->
        <item name="android:padding">16dp</item> <!-- Padding -->
    </style>

</resources>
